{
  "name": "dashboard",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://RachyHassan.github.io/alx-react",
  "dependencies": {
    "enzyme": "^3.11.0"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  }
}
